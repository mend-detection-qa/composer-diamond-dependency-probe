{
    "name": "mend-detection-qa/composer-diamond-dependency-probe",
    "description": "Minimal Composer probe: diamond-dependency detection test. Root requires monolog/monolog and symfony/cache, both of which pull in psr/log as a shared transitive dep.",
    "type": "project",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "monolog/monolog": "^3.0",
        "symfony/cache": "^6.0"
    },
    "config": {
        "preferred-install": "dist",
        "sort-packages": true,
        "allow-plugins": {}
    },
    "minimum-stability": "stable",
    "prefer-stable": true
}